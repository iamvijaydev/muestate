---
sidebar_position: 3
---

# Access store methods & state

Muestate store methods and state can be accessed using the `useStore` hook. The `useStore` hook returns a tuple with the current state and the store methods.

```ts
import { useStore } from 'muestate/react'